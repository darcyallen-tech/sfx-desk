# Optional Woosh DFlow engine (Sony AI). Install AFTER core setup_windows.bat.
# Weights are separate (~3.7 GB safetensors) — see scripts/download_woosh_weights.bat
# License on weights: CC BY-NC 4.0 (non-commercial outputs).
einops>=0.8.1
hydra-core>=1.3.2
omegaconf>=2.3.0
timm>=1.0.22
torchdiffeq>=0.2.5
transformers>=4.40.0
pydantic>=2.0
safetensors>=0.4.0
# Install the package itself from Sony's repo (pinned release):
#   pip install "woosh @ git+https://github.com/SonyResearch/Woosh.git@v1.0.0"
# Install package with: pip install --no-deps "woosh @ git+https://github.com/SonyResearch/Woosh.git@v1.0.0"
tqdm>=4.66.0

# Extra runtime deps (woosh installs with --no-deps to keep our torch cu128)
hear21passt==0.0.26
av>=16.1.0

